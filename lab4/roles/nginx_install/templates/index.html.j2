<html>
  <head><title>Lab4</title></head>
  <body>
    <h1>
      Hello from
      {{ ansible_distribution | default(host_os | default('Unknown')) }}
    </h1>

    <p>
      IP:
      {{ ansible_default_ipv4.address | default(host_ip | default(ansible_host)) }}
    </p>

    <p>Host: {{ inventory_hostname }}</p>
  </body>
</html>
